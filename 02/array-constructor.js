var colors = ['white', 'blue', 'red', 'black'];

var el = document.getElementById('color');
el.textContent = colors[10];

for (i = 0; i < colors.length; i++) {
    document.write(colors[i] + " ");    
}